CREATE TABLE User (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    role TEXT NOT NULL
);

getAllUsers:
SELECT *
FROM User
ORDER BY name;

insertUser:
INSERT INTO User(id, name, role)
VALUES (?1, ?2, ?3);

deleteUser:
DELETE FROM User
WHERE id = ?1;
